<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alva Corporate — Gestão de Licitações</title>
    <meta name="description" content="Gestão especializada de licitações públicas. Transformamos o processo licitatório em vantagem competitiva para sua empresa.">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&display=swap" rel="stylesheet">

    <style>
        :root {
            --color-black: #0A0A0A;
            --color-white: #F5F5F3;
            --color-gray-900: #111111;
            --color-gray-800: #1A1A1A;
            --color-gray-600: #444444;
            --color-gray-300: #AAAAAA;
            --color-gray-100: #EBEBEB;
            --transition: all 0.4s cubic-bezier(0.25, 0.1, 0.25, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            border-radius: 0 !important;
        }

        body {
            background-color: var(--color-black);
            color: var(--color-white);
            font-family: 'DM Sans', sans-serif;
            font-weight: 300;
            line-height: 1.8;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            scroll-behavior: smooth;
        }

        h1, h2, h3, .bebas {
            font-family: 'Bebas Neue', sans-serif;
            font-weight: 400;
            letter-spacing: 0.04em;
            text-transform: uppercase;
        }

        a {
            text-decoration: none;
            color: inherit;
            transition: var(--transition);
        }

        ul { list-style: none; }

        /* Typography & Logo */
        .logo { 
            font-family: 'Bebas Neue', sans-serif; 
            font-size: 28px; 
            letter-spacing: 0.12em; 
            color: var(--color-white); 
        }
        .logo-sub { 
            font-family: 'DM Sans', sans-serif; 
            font-size: 11px; 
            font-weight: 300; 
            letter-spacing: 0.35em; 
            color: var(--color-gray-300); 
            vertical-align: middle; 
            margin-left: 8px; 
        }

        /* Layout Utility */
        .container {
            max-width: 1300px;
            margin: 0 auto;
            padding: 0 4%;
        }

        .section-padding {
            padding: 140px 0;
        }

        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.8s ease-out, transform 0.8s ease-out;
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* Buttons */
        .btn {
            display: inline-block;
            padding: 18px 36px;
            font-family: 'DM Sans', sans-serif;
            font-size: 12px;
            font-weight: 500;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            cursor: pointer;
            border: 1px solid transparent;
            transition: var(--transition);
        }

        .btn-white {
            background-color: var(--color-white);
            color: var(--color-black);
        }

        .btn-white:hover {
            background-color: transparent;
            border-color: var(--color-white);
            color: var(--color-white);
        }

        .btn-outline {
            background-color: transparent;
            border-color: var(--color-white);
            color: var(--color-white);
        }

        .btn-outline:hover {
            background-color: var(--color-white);
            color: var(--color-black);
        }

        /* Navbar */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            height: 90px;
            display: flex;
            align-items: center;
            border-bottom: 1px solid var(--color-gray-800);
            transition: var(--transition);
            background-color: var(--color-black);
        }

        nav.scrolled {
            height: 75px;
            backdrop-filter: blur(10px);
            background-color: rgba(10, 10, 10, 0.9);
        }

        .nav-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
        }

        .nav-links {
            display: flex;
            gap: 40px;
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
        }

        .nav-links a {
            font-size: 11px;
            letter-spacing: 0.15em;
            font-weight: 400;
            color: var(--color-gray-300);
        }

        .nav-links a:hover {
            color: var(--color-white);
        }

        /* Hero */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            position: relative;
            overflow: hidden;
            background-color: var(--color-black);
        }

        .hero-ghost {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-family: 'Bebas Neue', sans-serif;
            font-size: 40vw;
            color: var(--color-white);
            opacity: 0.025;
            z-index: 0;
            pointer-events: none;
            white-space: nowrap;
        }

        .hero-content {
            position: relative;
            z-index: 1;
        }

        .hero-line {
            width: 60px;
            height: 1px;
            background-color: var(--color-white);
            margin-bottom: 30px;
        }

        .hero-title {
            font-size: clamp(64px, 10vw, 130px);
            line-height: 0.9;
            margin-bottom: 40px;
        }

        .hero-subtitle {
            max-width: 500px;
            font-size: 18px;
            color: var(--color-gray-100);
            margin-bottom: 50px;
        }

        .hero-btns {
            display: flex;
            gap: 20px;
        }

        /* Metrics (White Section) */
        .stats {
            background-color: var(--color-white);
            color: var(--color-black);
            padding: 100px 0;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            text-align: center;
        }

        .stat-item {
            border-right: 1px solid var(--color-gray-600);
            padding: 20px;
        }

        .stat-item:last-child {
            border-right: none;
        }

        .stat-number {
            font-size: 72px;
            display: block;
            line-height: 1;
        }

        .stat-label {
            font-size: 13px;
            letter-spacing: 0.2em;
            font-weight: 500;
            margin-top: 10px;
            display: block;
        }

        /* Services */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 0;
            margin-top: 60px;
        }

        .service-card {
            padding: 60px 40px;
            border: 1px solid var(--color-gray-600);
            transition: var(--transition);
            position: relative;
        }

        .service-card:hover {
            background-color: var(--color-white);
            color: var(--color-black);
            z-index: 2;
        }

        .service-num {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 32px;
            margin-bottom: 30px;
            display: block;
        }

        .service-title {
            font-size: 32px;
            margin-bottom: 20px;
            line-height: 1.1;
        }

        .service-desc {
            font-size: 15px;
            color: var(--color-gray-300);
            transition: var(--transition);
        }

        .service-card:hover .service-desc {
            color: var(--color-black);
        }

        /* Workflow */
        .workflow {
            background-color: var(--color-gray-900);
        }

        .section-header {
            text-align: center;
            margin-bottom: 100px;
        }

        .section-header .bebas {
            font-size: 60px;
            margin-bottom: 10px;
        }

        .timeline {
            max-width: 900px;
            margin: 0 auto;
            position: relative;
        }

        .timeline::before {
            content: '';
            position: absolute;
            left: 50px;
            top: 0;
            bottom: 0;
            width: 1px;
            background-color: var(--color-gray-600);
        }

        .timeline-step {
            position: relative;
            padding-left: 120px;
            margin-bottom: 80px;
        }

        .step-num {
            position: absolute;
            left: 0;
            top: -10px;
            font-family: 'Bebas Neue', sans-serif;
            font-size: 80px;
            line-height: 1;
            color: transparent;
            -webkit-text-stroke: 1px var(--color-gray-600);
        }

        .step-content h3 {
            font-size: 28px;
            margin-bottom: 15px;
        }

        /* Why Alva */
        .why {
            background-color: var(--color-white);
            color: var(--color-black);
        }

        .why-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 60px;
        }

        .why-item {
            border-top: 1px solid var(--color-black);
            padding-top: 30px;
        }

        .why-item h3 {
            font-size: 32px;
            margin-bottom: 20px;
        }

        /* CTA */
        .cta-section {
            text-align: center;
            background-color: var(--color-black);
            padding: 180px 0;
            position: relative;
        }

        .cta-title {
            font-size: clamp(40px, 8vw, 90px);
            margin-bottom: 60px;
            max-width: 900px;
            margin-left: auto;
            margin-right: auto;
        }

        .cta-underline {
            width: 200px;
            height: 2px;
            background-color: var(--color-white);
            margin: 40px auto 0;
            opacity: 0.3;
        }

        /* Footer */
        footer {
            padding: 100px 0 60px;
            border-top: 1px solid var(--color-gray-800);
        }

        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: 100px;
            margin-bottom: 80px;
        }

        .footer-tagline {
            margin-top: 30px;
            font-size: 14px;
            letter-spacing: 0.1em;
            color: var(--color-gray-300);
        }

        .footer-label {
            font-size: 12px;
            color: var(--color-gray-600);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-bottom: 30px;
            display: block;
        }

        .footer-links li {
            margin-bottom: 15px;
        }

        .copyright {
            padding-top: 40px;
            border-top: 1px solid var(--color-gray-800);
            font-size: 12px;
            color: var(--color-gray-600);
            display: flex;
            justify-content: space-between;
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .nav-links { display: none; }
            .why-grid { grid-template-columns: 1fr; gap: 40px; }
            .stats-grid { grid-template-columns: 1fr; }
            .stat-item { border-right: none; border-bottom: 1px solid var(--color-gray-600); padding: 40px 0; }
            .stat-item:last-child { border-bottom: none; }
            .footer-grid { grid-template-columns: 1fr; gap: 60px; }
        }

        .bg-texture {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: radial-gradient(var(--color-gray-600) 1px, transparent 1px);
            background-size: 40px 40px;
            opacity: 0.04;
            pointer-events: none;
        }
    </style>
</head>
<body>

    <nav id="navbar">
        <div class="container nav-content">
            <div class="logo-wrap">
                <span class="logo">ALVA</span><span class="logo-sub">CORPORATE</span>
            </div>
            <ul class="nav-links">
                <li><a href="#services">SERVIÇOS</a></li>
                <li><a href="#workflow">COMO FUNCIONA</a></li>
                <li><a href="#why">SOBRE</a></li>
                <li><a href="#footer">CONTATO</a></li>
            </ul>
            <a href="mailto:alvacorporate@gmail.com" class="btn btn-white" style="padding: 12px 24px; font-size: 10px;">FALAR COM ESPECIALISTA</a>
        </div>
    </nav>

    <main>
        <section class="hero" id="home">
            <div class="bg-texture"></div>
            <div class="hero-ghost">ALVA</div>
            <div class="container hero-content">
                <div class="hero-line"></div>
                <h1 class="hero-title reveal">LICITAÇÃO<br>É ESTRATÉGIA.</h1>
                <p class="hero-subtitle reveal">A Alva Corporate transforma complexidade em contratos de alto valor. Do edital à homologação, gestão técnica e jurídica especializada.</p>
                <div class="hero-btns reveal">
                    <a href="mailto:alvacorporate@gmail.com" class="btn btn-white">ANÁLISE GRATUITA</a>
                    <a href="#services" class="btn btn-outline">NOSSOS SERVIÇOS</a>
                </div>
            </div>
        </section>

        <section class="stats">
            <div class="container stats-grid reveal">
                <div class="stat-item">
                    <span class="stat-number">+500</span>
                    <span class="stat-label">EDITAIS ANALISADOS</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">+R$ 80M</span>
                    <span class="stat-label">EM CONTRATOS GERIDOS</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">98%</span>
                    <span class="stat-label">TAXA DE HABILITAÇÃO</span>
                </div>
            </div>
        </section>

        <section class="section-padding" id="services">
            <div class="container">
                <div class="section-header reveal">
                    <h2 class="bebas">SERVIÇOS DE ELITE</h2>
                    <p style="color: var(--color-gray-300);">Inteligência de mercado para vencer em editais públicos.</p>
                </div>
                <div class="services-grid">
                    <div class="service-card reveal">
                        <span class="service-num">I</span>
                        <h3 class="service-title">PROSPECÇÃO DE EDITAIS</h3>
                        <p class="service-desc">Monitoramento inteligente e filtragem de oportunidades alinhadas ao seu core business.</p>
                    </div>
                    <div class="service-card reveal">
                        <span class="service-num">II</span>
                        <h3 class="service-title">ANÁLISE JURÍDICA</h3>
                        <p class="service-desc">Estudo minucioso de riscos e viabilidade técnica para garantir segurança em cada lance.</p>
                    </div>
                    <div class="service-card reveal">
                        <span class="service-num">III</span>
                        <h3 class="service-title">GESTÃO DOCUMENTAL</h3>
                        <p class="service-desc">Organização e preparo rigoroso de toda a documentação necessária para habilitação sem falhas.</p>
                    </div>
                    <div class="service-card reveal">
                        <span class="service-num">IV</span>
                        <h3 class="service-title">ACOMPANHAMENTO</h3>
                        <p class="service-desc">Suporte integral durante as fases de lances, recursos administrativos e homologação.</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="section-padding workflow" id="workflow">
            <div class="container">
                <div class="section-header reveal">
                    <h2 class="bebas">METODOLOGIA</h2>
                    <p style="color: var(--color-gray-300);">Nosso caminho para a vitória.</p>
                </div>
                <div class="timeline">
                    <div class="timeline-step reveal">
                        <span class="step-num">01</span>
                        <div class="step-content">
                            <h3 class="bebas">DIAGNÓSTICO</h3>
                            <p style="color: var(--color-gray-300);">Analisamos sua empresa e o mercado para identificar o cenário competitivo real.</p>
                        </div>
                    </div>
                    <div class="timeline-step reveal">
                        <span class="step-num">02</span>
                        <div class="step-content">
                            <h3 class="bebas">ESTRATÉGIA</h3>
                            <p style="color: var(--color-gray-300);">Desenvolvemos o plano de ação, precificação e mapeamento de concorrentes.</p>
                        </div>
                    </div>
                    <div class="timeline-step reveal">
                        <span class="step-num">03</span>
                        <div class="step-content">
                            <h3 class="bebas">EXECUÇÃO</h3>
                            <p style="color: var(--color-gray-300);">Operacionalização técnica completa da participação no pregão ou concorrência.</p>
                        </div>
                    </div>
                    <div class="timeline-step reveal">
                        <span class="step-num">04</span>
                        <div class="step-content">
                            <h3 class="bebas">RESULTADO</h3>
                            <p style="color: var(--color-gray-300);">Homologação do contrato e acompanhamento pós-licitatório para máxima eficiência.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="section-padding why" id="why">
            <div class="container">
                <div class="section-header reveal">
                    <h2 class="bebas">POR QUE ALVA?</h2>
                </div>
                <div class="why-grid">
                    <div class="why-item reveal">
                        <h3 class="bebas">EXPERTISE TÉCNICA</h3>
                        <p>Nossa equipe é formada por especialistas que conhecem as nuances jurídicas que desclassificam amadores.</p>
                    </div>
                    <div class="why-item reveal">
                        <h3 class="bebas">FOCO EM RESULTADO</h3>
                        <p>Não participamos para estar lá. Participamos para vencer. Nossa remuneração é atrelada ao seu sucesso.</p>
                    </div>
                    <div class="why-item reveal">
                        <h3 class="bebas">ZERO BUROCRACIA</h3>
                        <p>Assumimos toda a carga operacional para que você foque apenas na entrega do seu produto ou serviço.</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="cta-section">
            <div class="container reveal">
                <h2 class="cta-title bebas">PRONTO PARA VENCER CONTRATOS?</h2>
                <a href="mailto:alvacorporate@gmail.com" class="btn btn-white">FALAR COM UM CONSULTOR</a>
                <div class="cta-underline"></div>
            </div>
        </section>
    </main>

    <footer id="footer">
        <div class="container">
            <div class="footer-grid">
                <div class="footer-brand">
                    <span class="logo">ALVA</span><span class="logo-sub">CORPORATE</span>
                    <p class="footer-tagline">LICITAÇÃO ESTRATÉGICA.<br>RESULTADO REAL.</p>
                </div>
                <div class="footer-nav">
                    <span class="footer-label">Navegação</span>
                    <ul class="footer-links">
                        <li><a href="#services">Serviços</a></li>
                        <li><a href="#workflow">Metodologia</a></li>
                        <li><a href="#why">Sobre Nós</a></li>
                    </ul>
                </div>
                <div class="footer-contact">
                    <span class="footer-label">Contato</span>
                    <ul class="footer-links">
                        <li>alvacorporate@gmail.com</li>
                        <li>+55 19 99896-9354</li>
                        <li>Piracicaba/SP</li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                <span>&copy; 2024 ALVA CORPORATE. TODOS OS DIREITOS RESERVADOS.</span>
                <span>DESENVOLVIDO POR ALVA DESIGN UNIT.</span>
            </div>
        </div>
    </footer>

    <script>
        const navbar = document.getElementById('navbar');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) { navbar.classList.add('scrolled'); } 
            else { navbar.classList.remove('scrolled'); }
        });

        const observerOptions = { threshold: 0.1 };
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) { entry.target.classList.add('active'); }
            });
        }, observerOptions);

        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) target.scrollIntoView({ behavior: 'smooth' });
            });
        });
    </script>
</body>
</html>